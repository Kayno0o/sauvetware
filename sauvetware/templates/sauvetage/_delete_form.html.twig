<form method="post" action="{{ path('sauvetage_delete', {'id': sauvetage.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ sauvetage.id) }}">
    <button class="btn">Delete</button>
</form>
